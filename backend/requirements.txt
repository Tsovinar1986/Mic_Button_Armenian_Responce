# Web framework serving /api/chat, /api/speak, /api/transcribe, /api/health,
# /api/models, and the built frontend static files.
fastapi==0.116.1
uvicorn[standard]==0.35.0

# Lets FastAPI parse multipart/form-data uploads (used by /api/transcribe
# for the recorded-audio file from the browser).
python-multipart==0.0.20

# HTTP client used to call the local Ollama server (/api/chat, /api/models,
# /api/health).
requests==2.32.3

# ML runtime and model-loading libraries, used for both local models:
# - Armenian text-to-speech: facebook/mms-tts-hyw (VITS model)
# - Speech-to-text fallback for Safari/Firefox: faster-whisper
torch==2.7.1
transformers==4.57.6
accelerate==1.7.0
huggingface_hub==0.34.0
sentencepiece==0.2.0

# Audio/array handling for the TTS output (waveform -> WAV bytes).
scipy==1.14.1
numpy==2.1.3

# Local Whisper implementation (CTranslate2-based, faster/lighter than
# openai-whisper) used by /api/transcribe.
faster-whisper==1.2.1
